// =====================================================================
//  NOVE Project - Loading Screen
//  Единственный файл, который нужно править под свой сервер.
// =====================================================================

const CONFIG = {

    // ===========================
    // Основная информация
    // ===========================

    // Если оставить пустым (""), название подставится реальное,
    // то самое, что GMod передаёт со стороны сервера.
    serverName: "NOVE PROJECT",

    subtitle: "Добро пожаловать.",

    version: "Version 1.0",


    // Показывать карту / режим / слоты, полученные от сервера
    showServerDetails: true,


    // Что именно считает счётчик под полосой. На этапе Workshop движок
    // передаёт количество аддонов коллекции, а не отдельных файлов.
    // Три формы для русского счёта: 1 аддон, 2 аддона, 5 аддонов.
    countUnit: ["аддон", "аддона", "аддонов"],


    // ===========================
    // Оформление
    // ===========================

    theme: {

        accent: "#E04B7B",      // фирменный розовый (взят из логотипа)
        accent2: "#5FAEE2"      // фирменный синий (взят из логотипа)
    },


    // Логотип. Оставьте "" — будет использован встроенный SVG (файлы не нужны).
    // Если укажете путь ("img/logo.png") — картинка подставится ТОЛЬКО если реально
    // загрузится; при 404 останется SVG, битой иконки не будет.
    logo: "",

    // Фоновая картинка. Оставьте "" — будет CSS-фон (градиент + сетка).
    // Работает по тому же принципу: не загрузилась — тихо остаётся CSS-фон.
    background: "img/background.jpg",

    // Медленное приближение фона. Для арта, в который уже вписан логотип,
    // лучше false — иначе зумом подрезает края логотипа.
    backgroundZoom: false,

    // Показывать логотип, название и подзаголовок по центру экрана.
    // На нашем фоне логотип и надпись NOVE PROJECT уже нарисованы,
    // поэтому false — чтобы не было двух логотипов друг на друге.
    showBranding: false,


    // ===========================
    // Музыка
    // ===========================
    //
    // ВАЖНО: браузер внутри GMod (CEF) блокирует автозапуск звука без действия
    // пользователя. Скрипт пытается запустить и повторяет попытку при любом
    // клике/нажатии клавиши. Если звука нет — это ограничение CEF, не баг.

    music: {

        enabled: false,
        file: "audio/music.mp3",
        volume: 0.35
    },


    // ===========================
    // Новости
    // ===========================

    news: [

        "🚇 Добро пожаловать на NOVE Project!",
        "⚙ Используйте качественный микрофон.",
        "🛠 Соблюдайте правила проекта.",
        "❤️ Спасибо за игру!"
    ],


    // ===========================
    // Советы (меняются внизу экрана)
    // ===========================

    tips: [

        "Перед отправлением убедитесь, что двери закрыты.",
        "Следите за показаниями АРС.",
        "Не открывайте двери до полной остановки.",
        "Соблюдайте интервалы движения.",
        "Не нарушайте правила проекта.",
        "Уважайте других игроков."
    ],

    tipInterval: 8000,      // мс между сменой совета


    // ===========================
    // Ссылки (пустые не показываются)
    // ===========================

    links: {

        discord: "",
        telegram: "",
        website: ""
    },


    // ===========================
    // Демо-режим
    // ===========================
    //
    // "auto"  - имитация загрузки ТОЛЬКО когда страница открыта в обычном браузере
    //           (например, на GitHub Pages для проверки вёрстки). Внутри GMod
    //           всегда используются настоящие данные.
    // false   - никогда не имитировать.
    // true    - всегда имитировать.

    demo: "auto"

};
